program  = { commands ~ linebreaks }

commands = _{ (command ~ SEPARATOR) ~ commands
            | command }
command  = { word* }

word       = _{ (ASCII_ALPHANUMERIC | "_" | "-" | ".")+ }
linebreaks = _{ (NEWLINE)* }

SEPARATOR  = _{ ";" }
NEWLINE    = _{ "\n" }
WHITESPACE = _{ " " | "\t" }
